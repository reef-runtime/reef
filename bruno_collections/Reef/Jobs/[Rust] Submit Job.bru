meta {
  name: [Rust] Submit Job
  type: http
  seq: 2
}

post {
  url: {{url}}/api/jobs/submit
  body: json
  auth: none
}

body:json {
  {
    "name": "Bruno test job",
    "datasetId": null,
    "sourceCode": "pub fn run(dataset: &[u8]) { let msg = \"Hello World!\"; reef::reef_log(msg); reef::reef_log(&format!(\"dataset[42]: {:?}\", dataset[42])); println!(\"Println log.\"); if dataset[0] == 13 { panic!(\"Bad dataset!\"); } }",
    "language": "rust"
  }
}
